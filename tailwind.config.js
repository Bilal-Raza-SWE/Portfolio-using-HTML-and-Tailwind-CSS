/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './*.html',  // Adjust this path if your HTML files are in subdirectories
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
